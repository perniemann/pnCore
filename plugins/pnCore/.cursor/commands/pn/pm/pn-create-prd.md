---
name: pn-create-prd
description: Create a Product Requirements Document — 8-section template or feature-matrix for multi-phase products. Use before pn-writing-plans and build when stakeholders need a spec.
---

# pn-create-prd

**Start every response with:** `[pn-command] 🔺`

Load and run `get_skill("pn-create-prd")`.

## When to use

| Signal | Use |
|--------|-----|
| Writing a PRD before implementation | Yes |
| Backlog only, spec exists | No — `/pn-user-stories` |
| Startup viability only | No — `/pn-pressure-test` or `/pn-strategy` |

## Next steps

After approval: `/pn-user-stories` → `pn-writing-plans` → `/pn-build`.
